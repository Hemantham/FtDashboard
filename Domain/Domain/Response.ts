 // Find all classes with a name ending with Model
    class Response {
        constructor(public responseType: number, public responseId: string, public inputId: number, public email: string, public completionDate: Date, public answer: string, public question: Question) {
        }
    }
 