 // Find all classes with a name ending with Model
    class ChartEntry {
        constructor(public value: any, public xAxisLable: string, public xAxisValue: any, public xAxisId: number, public series: string) {
        }
    }
 